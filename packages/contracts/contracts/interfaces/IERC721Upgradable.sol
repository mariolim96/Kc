// SPDX-License-Identifier: MIT

// Define the SPDX license identifier, and import any necessary interfaces from OpenZeppelin Contracts or other dependencies.

pragma solidity ^0.8.20;

// import {IERC721} from '@openzeppelin/contracts/token/ERC721/IERC721.sol';
// import {IERC721Metadata} from '@openzeppelin/contracts/token/ERC721/extensions/IERC721Metadata.sol';
// import {IERC721Receiver} from '@openzeppelin/contracts/token/ERC721/IERC721Receiver.sol';
// import {IERC165} from '@openzeppelin/contracts/utils/introspection/IERC165.sol';

interface IERC721Upgradeable {
    // // Declare the function signatures you want to include in the interface.
    // function name() external view returns (string memory);
    // function symbol() external view returns (string memory);
    // function tokenURI(uint256 tokenId) external view returns (string memory);
    // function balanceOf(address owner) external view returns (uint256);
    // function ownerOf(uint256 tokenId) external view returns (address);
    // function getApproved(uint256 tokenId) external view returns (address);
    // function approve(address to, uint256 tokenId) external;
    // function setApprovalForAll(address operator, bool approved) external;
    // function isApprovedForAll(address owner, address operator) external view returns (bool);
    // function transferFrom(address from, address to, uint256 tokenId) external;
    // function safeTransferFrom(address from, address to, uint256 tokenId) external;
    // function safeTransferFrom(address from, address to, uint256 tokenId, bytes calldata data) external;
}
